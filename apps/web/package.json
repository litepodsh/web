{
  "name": "litepod-web",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test:landing": "node scripts/test-landing.mjs",
    "format": "oxfmt --write . && biome format --write src",
    "format:check": "oxfmt --check . && biome format src",
    "lint": "oxlint ."
  },
  "dependencies": {
    "@astrojs/node": "^11.1.4",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.4",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.11",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
